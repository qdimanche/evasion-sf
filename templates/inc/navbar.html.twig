<nav class="navbar navbar-expand-lg navbar-light bg-dark">
   <a href="/" class="d-flex align-items-center mb-2 mb-lg-0 text-white text-decoration-none"
                    style="padding-bottom: 15px;">
                    <img src="img/logo.png" class="bi me-2" style="height:40px"></img>
                </a>
  <button class="navbar-toggler" type="button" data-toggle="collapse" data-target="#navbarSupportedContent" aria-controls="navbarSupportedContent" aria-expanded="false" aria-label="Toggle navigation">
    <span class="navbar-toggler-icon"></span>
  </button>

  <div class="collapse navbar-collapse" id="navbarSupportedContent">
    <ul class="navbar-nav ml-4 mr-auto">
      <li class="nav-item active">
        <a class="nav-link text-white" href="#">Accueil <span class="sr-only">(current)</span></a>
      </li>
    </ul>
                {% if is_granted('ROLE_USER') %}
            <a href="{{ path('new') }}" class="btn btn-secondary btn-lg active" role="button" aria-pressed="true" id="new-btn">+</a>
            {% endif %}
            <form action="/">
                <input type="text" name="search" placeholder="Rechercher..." class="p-1">
            </form>
            {% if is_granted('IS_AUTHENTICATED_FULLY') %}
                <a href="{{ path('logout') }}" class="btn btn-primary btn-md active" role="button" aria-pressed="true" id="logout-btn" style="margin-left:30px;">Se déconnecter</a>
                {% else %}
                <a href="{{ path('app_login') }}" class="btn btn-primary btn-md active" role="button" aria-pressed="true" id="login-btn" style="margin-left:30px;">Se connecter</a>
                <a href="{{ path('app_register') }}" class="btn btn-secondary btn-md active" role="button" aria-pressed="true" id="signup-btn" style="margin-left:30px;" git>S'inscrire</a>
                {% endif %}
 
  </div>


</nav>